# How to Write Documentation

## English
To add new documentation, create a Markdown file in `docs/en/` and its translation in `docs/es/`.
Then access it via `?page=docs&doc=your-file-name`.

### Formatting
- Use standard Markdown.
- Headings start with `#`.
- Links use `[text](url)`.
